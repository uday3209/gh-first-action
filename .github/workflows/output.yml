name: Output Workflow
on: workflow_dispatch
jobs:
    info: 
        runs-on: ubuntu-latest
        steps:
          - name: Output Github Context
            run: |
            echo "Hello, This is uday"
            echo "${{ toJSON(github) }}"
